# Legacy password login (used unless the ENTRA_* block below is fully set).
API_USERNAME=claudeclawos
API_PASSWORD=R!sc2023
API_BASE_URL=http://localhost:8005

# --- Entra ID client-credentials login (optional) ---
# When all four are set, this server authenticates via the "IT Ticketing
# System - MCP Service" app registration instead of the password above.
# See backend/entra_auth.py's validate_service_token for the other side.
ENTRA_TENANT_ID=
ENTRA_MCP_CLIENT_ID=
ENTRA_MCP_CLIENT_SECRET=
# Client ID of "IT Ticketing System - Web" - the API this token is
# actually requested for (api://<this-id>/.default), not Microsoft Graph.
ENTRA_WEB_APP_CLIENT_ID=
